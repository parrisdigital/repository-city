# Optional but recommended for higher GitHub API limits.
# This value is server-only and must never be prefixed with NEXT_PUBLIC_.
GITHUB_TOKEN=

# Canonical production URL used by metadata and sharing.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
